{
  "name": "@keyless/web-components",
  "version": "0.1.0",
  "main": "dist/src/NoteItem.js",
  "module": "dist/src/NoteItem.js",
  "types": "dist/src/NoteItem.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build && tsc --emitDeclarationOnly --outDir dist/src",
    "dev": "vite build --watch && tsc --emitDeclarationOnly --outDir dist/src",
    "clean": "rm -rf dist"
  },
  "exports": {
    "./note-item": {
      "import": "./dist/src/NoteItem.js",
      "types": "./dist/src/NoteItem.d.ts"
    }
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@keyless/eslint-config": "workspace:*",
    "@keyless/typescript-config": "workspace:*",
    "typescript": "~5.5.0",
    "vite": "^5.0.0"
  }
}
